/**
 * @format
 */

import {AppRegistry} from 'react-native';
import FinalProject from './FinalProject.tsx';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => FinalProject);
